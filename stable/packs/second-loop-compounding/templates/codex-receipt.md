# <题> · 回执（Codex → Claude）· <YYYY-MM-DD>

<!-- 回执模板（契约 <项目>/<交接契约>，要点见本套装 docs/framework.md §9）。写到交接包 §3「回执」那一行指定的路径。
     五节都要有：没内容的节写「无」，不删节——Claude 按节收货；若 <项目> 的派单壳会查回执：缺节会被报出，
     回执文件不在则整单记失败（零回执）。写事实不写感想：路径、条数、命令、做了没做。
     来历：抽自第一个真实样本的回执（那份是四段散文，本模板把它拆成五节）。 -->

> 交接包：`<仓内路径>` · 派法：<派单壳 run_id / 人在 Codex 里手动派> · 用时：<开始–结束>

## 1 产出路径

> 写什么：每个产物一行仓内路径 + 一句是什么 + 怎么打开（双击 / 起服务）+ 数据怎么更新（换哪个文件、点哪个入口）。为什么：Claude 收货第一步是按这里找东西，路径不全就得翻你的对话记录。

- `<仓内路径>` —— <是什么；怎么打开；怎么更新数据>

## 2 对脚手架的推翻与理由

> 写什么：推翻了交接包 §7 脚手架（或 §4 以外的呈现建议）的哪几条，一条一行：原条 → 改成 → 为什么；没推翻写「无」。为什么：推翻是你的权利，不说出来就是沉默的偏离——样本的三处推翻（file:// 读不到邻近 JSON、手机没有 hover、勾选 ≠ 授权）都因为说清了理由，Claude 才能直接收。

| 原条（§7 哪一行） | 改成 | 为什么 |
|---|---|---|
| <内容项 / 原建议> | <你的做法> | <原建议在这里为什么不成立> |

## 3 没做什么与为什么

> 写什么：包里要了但没做的、做不到的、刻意不做的；包里内容缺或自相矛盾的地方（写清缺在哪，交回 Claude 补包，不自己补）。为什么：「没做」写出来是交接，不写出来是事故。

- <没做的事 / 内容缺口> —— <为什么；需要谁补什么>

## 4 验收项逐条自查

> 写什么：交接包 §5 验收清单逐条对一遍，不许跳条；状态只用三档——**做了**（附怎么验的）/ **只提了**（页面上有入口或说明，但没真正实现）/ **没做**（附原因）。为什么：Claude 收货时会逐条复跑，你的自查是它的对照表；「只提了」单列，是为了不让它冒充「做了」。

| # | 验收项（照抄 §5） | 状态 | 怎么验的 / 为什么没做 |
|---|---|---|---|
| 1 | <…> | 做了 / 只提了 / 没做 | <…> |

## 5 未入库文件清单

> 写什么：这次新建或改动、但不会进 git 的文件——被 .gitignore 兜住的、在临时目录的、依赖本机才有的东西（如临时目录里的 jsdom）；每个一行：路径 + 为什么没入库 + 丢了会怎样；没有写「无」。为什么：样本的页面、数据、构建脚本全被 .gitignore 兜住只在本机，不列出来，Claude 会以为已经入库。

- `<路径>` —— <被 .gitignore 第几行兜住 / 在临时目录 / 依赖本机> —— <丢了会怎样>

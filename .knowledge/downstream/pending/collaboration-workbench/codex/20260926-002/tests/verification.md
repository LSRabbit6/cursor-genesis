# 验证记录 · 2026-09-26

## 环境

Node.js 24.19.0、Python 3.12.14，macOS。本次没有迁入真实业务资料、旧菜单票据或令牌。集成测试使用临时项目和临时数据库；浏览器工具验证仅创建了一条明确标为“本地接口验收记录，不是业务需求”的本机记录，不进入线上实例。

## 已通过

- `node --test service/tests/*.test.mjs`：8 项通过，failed / error 为 0。
- 包含真实 HTTP 服务 + 独立 Python 客户端的提交、校验下载、安装、版本读取、证据回写、重启后查询；不是仅模拟返回值。
- 接口覆盖：持久化、并发重试幂等、身份隔离、项目令牌权限、撤销、状态冲突、发布包约束、分页 / 搜索、跨站拒绝、输入体积限制。
- 页面逻辑覆盖：自查与反馈表单、断线保留输入、重复提交沿用回执、详情、维护操作、执行证据、筛选、注入字符串仅按文字显示。
- `python -m unittest discover -s scripts/tests -q`：19 项通过，failed / error 为 0。保留既有安装器与宿主适配回归，并增加客户端归档、摘要、地址与离线补交测试。
- 原安装器 Mac 对 Windows 路径的两处失败通过实现修正消除，没有降低测试判据。
- 产物规范检查：`web/index.html`、`docs/collaboration-map.html` 均 PASS，0 FAIL。
- WebMCP 实际宿主：两个工具注册成功；列表与详情调用显示本机验收记录；非法状态与非法编号分别被拒绝。该项只验证结构化工具契约，不代替全面浏览器视觉验收。
- `pnpm build`：可生成 Worker、静态资源、校验包、清单与迁移文件。
- `git diff --check`：通过。

## 边界

- 手机样式、长内容换行和键盘焦点已实现；本次没有执行浏览器截图与多宽度视觉验收，不报告为已通过。
- 线上部署成功与否以发布平台回执为准；本记录不提前宣称已部署。
- 私有 Sites 受外层网页登录保护，普通 CLI 无法仅靠项目令牌穿过该层；完整 CLI 链路已在本机服务验证。网页可操作同一实例的自查、反馈、处理、取包与证据。
- 所有执行证据是项目提交者的记录；服务不代跑检查，不据此推断宿主自动触发或业务验收通过。
